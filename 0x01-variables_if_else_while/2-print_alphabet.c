#include <stdio.h>


/** 
 * main - Prints out the alphabet in small letters
 * when it executes
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
